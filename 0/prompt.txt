Review follow-up: replace the implicit uv-before-yamllint ordering based on [settings] jobs = 1 and TOML declaration order with mise's explicit tool dependency mechanism. yamllint resolves through the pipx backend and requires uv, so declare yamllint with depends = uv, remove the global jobs = 1 serialization unless independently necessary, and update the regression test to assert the semantic dependency while preserving checkout/global uv version consistency. Re-run the existing clean-room/bootstrap validation and compatibility gate.